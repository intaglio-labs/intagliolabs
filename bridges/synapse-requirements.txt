# Synapse's LOCKED dependency set for the native (no-Docker) homeserver.
#
# WHY A LOCKFILE, and it is not tidiness. matrix-synapse declares
# `prometheus-client >=0.6.0` -- an open lower bound -- so a fresh
# `pip install matrix-synapse==1.140.0` resolves to whatever is newest, which is
# 0.26.0, which cannot be subclassed the way Synapse subclasses Collector:
#
#     TypeError: Cannot create a consistent method resolution order (MRO)
#                for bases Generic, Collector
#
# That is not a macOS problem and not a Python-version problem -- it reproduces
# identically on 3.12 and 3.14. The container never hit it because an image
# carries a resolved dependency set frozen at build time. Moving off Docker means
# taking that job over, and this file IS that job.
#
# Generated by installing matrix-synapse==1.140.0 on CPython 3.12 with
# --only-binary=:all: and pinning prometheus_client to the last version Synapse
# can subclass, then `pip freeze`. Regenerate the same way; do not hand-edit one
# line, because the set is only known-good as a set.
#
# Verified 2026-08-29 on aarch64-apple-darwin: config generated, homeserver
# started, /health answered OK, /_matrix/client/versions served through v1.12.
annotated-types==0.8.0
attrs==26.1.0
Automat==25.4.16
bcrypt==5.0.0
bleach==6.4.0
canonicaljson==2.0.0
cffi==2.1.1
constantly==23.10.4
cryptography==50.0.1
hyperlink==21.0.0
idna==3.19
ijson==3.5.1
immutabledict==4.3.1
Incremental==24.11.0
Jinja2==3.1.6
jsonschema-specifications==2025.9.1
jsonschema==4.26.0
MarkupSafe==3.0.3
matrix-common==1.3.0
matrix-synapse==1.140.0
msgpack==1.2.2
multipart==2.0.0
netaddr==1.3.0
packaging==26.3
phonenumbers==9.0.38
pillow==12.3.0
prometheus_client==0.21.1
pyasn1_modules==0.4.2
pyasn1==0.6.4
pycparser==3.0
pydantic_core==2.46.5
pydantic==2.13.5
pymacaroons==0.13.0
PyNaCl==1.6.2
pyOpenSSL==26.4.0
python-multipart==0.0.32
PyYAML==6.0.3
referencing==0.37.0
rpds-py==2026.6.3
semantic-version==2.10.0
service-identity==26.1.0
setuptools-rust==1.13.0
setuptools==84.0.0
signedjson==1.1.4
six==1.17.0
sortedcontainers==2.4.0
treq==26.7.0
Twisted==26.4.0
typing_extensions==4.16.0
typing-inspection==0.4.4
unpaddedbase64==2.1.0
webencodings==0.6.1
zope.interface==8.6

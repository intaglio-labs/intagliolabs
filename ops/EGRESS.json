{
  "_readme": [
    "THE EGRESS LEDGER. Every host this repo can reach is enumerated here, and",
    "here ONLY. This file exists because the same claim used to be written out",
    "longhand in nine prose files -- ui/AGENTS.md, connectors/AGENTS.md,",
    "README.md, four sections of site/privacy/index.html, connect/lib/status.mjs,",
    "widget/ui/connections.js -- and by 2026-08-22 every one of them was false.",
    "Notion, six chat platforms, two container registries and a model host had",
    "arrived since the list was written, and a decision recorded in one prose",
    "file cannot propagate to eight others. The widget ended up asserting the",
    "OPPOSITE of the governing document.",
    "",
    "So: prose points HERE. It does not restate this. If you are about to write",
    "a sentence that counts the paths, link this file instead.",
    "",
    "connectors/test/egress.test.mjs fails on any host literal in tracked source",
    "that is not listed below. That is the enforcement -- it fires on the commit",
    "that opens the socket, which is when ui/AGENTS.md's ordering rule says to",
    "catch it ('get this exemption text updated BEFORE the first commit that",
    "opens a non-loopback socket -- not after'). That rule was broken at least",
    "four times before this file existed.",
    "",
    "ADDING A PATH: add it here, in the same commit as the code, with a real",
    "`decision` -- and if no real decision exists, write UNRECORDED rather than",
    "inventing one -- never-fabricate binds this file too.",
    "",
    "WHAT THIS FILE DOES NOT DO: it records what the code CAN reach. It is not",
    "a claim that each path is justified, and it is not consent. Read `decision`",
    "on each entry.",
    "",
    "WHAT `decision` MEANS HERE. In the private repository it recorded that the",
    "owner had approved a path, and 'UNRECORDED' flagged one nobody had signed",
    "off. Open source has no single owner, so it now records WHY a path exists",
    "and WHO opens it: a user-enabled source, a build-time download, a link",
    "handed to the user's browser. If you add a path, say which.",
    "",
    "SCOPE. This ledger describes THIS repository. One entry was removed when the",
    "code was extracted for open source: the outbound iMessage lane, which was the",
    "courier's send path and is not here. A ledger that over-declares is as",
    "misleading as one that under-declares — it invites a reader to believe a",
    "capability exists.",
    "",
    "2026-08-24. The marketing site MOVED INTO this repository, at site/. It reaches",
    "EIGHT hosts, seven of them declared below under kind `website`. The seventh is huggingface.co, which already had an entry as a `model-asset` -- one host, two contexts, so filtering by kind alone under-counts the site. They were",
    "removed on 2026-08-22 when site/ was a separate artifact; that sentence is now",
    "false and has been rewritten rather than left standing. connectors/test/",
    "egress.test.mjs promoted `site` out of ROOTS_ELSEWHERE and into ROOTS in the",
    "same commit, which is what its floor test exists to force: a root that arrives",
    "unscanned fails the suite. Note what these entries are NOT -- no process in",
    "this repo opens them. They are what a VISITOR's browser fetches or follows.",
    "",
    "2026-08-23. The login-webview kind was DEFINED here and had ZERO entries, while the in-app login had been navigating Meta's hosts since it shipped. The tripwire could not see them: allowedSuffixes was a Swift array of BARE host strings ('instagram.com'), and the scan matches https?://host. So a host list that is a fence rather than a URL is invisible to it -- worth knowing, because a fence is exactly a list of places the code can go. The five entries added here close that, and connect/test/bridgePolicy.test.mjs now asserts each platform's loginUrl is inside its own fence."
  ],
  "version": 1,
  "last_reviewed": "2026-08-24",
  "_kinds": {
    "api": "the daemon or server opens this socket itself, on a timer or on demand",
    "bridge": "a local container holds a standing authenticated session to this platform",
    "registry": "container image pull, at install/upgrade time only",
    "login-webview": "an in-app WKWebView loads this so the owner can log in; cookies are harvested from it",
    "model-asset": "model or vendor asset download",
    "user-browser-link": "NOT this program's egress -- a link handed to the owner's own browser",
    "website": "a host the public marketing site in site/ reaches: a subresource the visitor's browser loads, or a link it hands them. Not the product's egress -- no daemon opens these",
    "loopback": "never leaves the machine"
  },
  "_the_claim": [
    "The narrow claim this system actually keeps, stated once:",
    "HAZLIE SENDS NO CORPUS TEXT TO A CLOUD MODEL. All reasoning and narration",
    "happen locally, on loopback llama-server instances.",
    "",
    "That is much narrower than 'nothing leaves this Mac', which is false and",
    "must not be written anywhere. Data leaves on every `api`, `bridge` and",
    "`login-webview` path below. What does not leave is corpus text on a model",
    "call: ui/server/hermes.mjs' sealed reader holds the only DB handle, carries",
    "no cloud credential, and talks only to its own loopback llama-server."
  ],
  "paths": [
    {
      "host": "api.notion.com",
      "kind": "api",
      "component": "connectors/lib/notionClient.mjs",
      "direction": "inbound-fetch",
      "purpose": "fetch the owner's own Notion pages",
      "credential": "notion internal integration token",
      "decision": "User-enabled source. Off until the user connects it on the connect page, and it reaches only that user's own account. Was UNRECORDED while this lived in a private repo, where `decision` meant 'the owner wrote down that they approved this' — a question with no meaning in a public one.",
      "evidence": "connectors/lib/notionClient.mjs:18 NOTION_BASE_URL"
    },
    {
      "host": "api.ouraring.com",
      "kind": "api",
      "component": "connectors/lib/ouraClient.mjs",
      "direction": "inbound-fetch",
      "purpose": "fetch back the owner's own ring data",
      "credential": "oura oauth2 access token",
      "decision": "ui/AGENTS.md egress policy, amended 2026-08-19, replacing the Health Auto Export LAN path",
      "evidence": "connectors/lib/ouraClient.mjs"
    },
    {
      "host": "oauth2.googleapis.com",
      "kind": "api",
      "component": "connectors/lib/gcalClient.mjs, connectors/lib/gmailClient.mjs",
      "direction": "outbound-send",
      "purpose": "google oauth2 token exchange and refresh",
      "credential": "google refresh token",
      "decision": "ui/AGENTS.md egress policy, amended 2026-08-19",
      "evidence": "connectors/lib/gcalClient.mjs"
    },
    {
      "host": "public-api.granola.ai",
      "kind": "api",
      "component": "connectors/lib/granolaClient.mjs",
      "direction": "inbound-fetch",
      "purpose": "fetch back transcripts and summaries Granola already holds",
      "credential": "granola api key",
      "decision": "ui/AGENTS.md egress policy, 2026-08-19. REST endpoints only -- the MCP query_* tools run inference server-side and are NOT approved",
      "evidence": "connectors/lib/granolaClient.mjs"
    },
    {
      "host": "www.googleapis.com",
      "kind": "api",
      "component": "connectors/lib/gcalClient.mjs",
      "direction": "inbound-fetch",
      "purpose": "read the owner's own calendar and mailbox. Calendar: the owner's calendar lives in Notion Calendar, which speaks to Google directly and never syncs into Calendar.app. Mail: replaced IMAP on 2026-08-26 — Google will not accept gmail.readonly over IMAP, which demands the full-mailbox scope (read, write, delete and send), so reaching mail over REST is what keeps the grant read-only",
      "credential": "google access token, scopes calendar.readonly and gmail.readonly — read-only in both: cannot create, move or delete an event, and cannot send, delete or even mark a message read",
      "decision": "ui/AGENTS.md egress policy, amended 2026-08-19",
      "evidence": "connectors/lib/gcalClient.mjs"
    },
    {
      "host": "discord.com",
      "kind": "bridge",
      "component": "bridges/ mautrix-discord + connect/lib/bridge.mjs",
      "direction": "bidirectional",
      "purpose": "Discord DMs through a local Matrix bridge",
      "credential": "harvested session tokens",
      "decision": "User-enabled source. Off until the user connects it on the connect page, and it reaches only that user's own account. Was UNRECORDED while this lived in a private repo, where `decision` meant 'the owner wrote down that they approved this' — a question with no meaning in a public one.",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.discord; bridges/docker-compose.yml image dock.mau.dev/mautrix/discord"
    },
    {
      "host": "slack.com",
      "kind": "bridge",
      "component": "bridges/ mautrix-slack + connect/lib/bridge.mjs",
      "direction": "bidirectional",
      "purpose": "Slack DMs through a local Matrix bridge",
      "credential": "harvested session tokens",
      "decision": "User-enabled source. Off until the user connects it on the connect page, and it reaches only that user's own account. Was UNRECORDED while this lived in a private repo, where `decision` meant 'the owner wrote down that they approved this' — a question with no meaning in a public one.",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.slack; bridges/docker-compose.yml image dock.mau.dev/mautrix/slack"
    },
    {
      "host": "app.slack.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift + connect/lib/bridge.mjs PLATFORMS.slack",
      "direction": "bidirectional",
      "purpose": "Slack's web client, which the login window walks to after a successful sign-in because it is where the session's client token lives.",
      "credential": "the owner's own Slack session, already established by the sign-in they just completed in that window.",
      "decision": "Already covered by the slack.com entry in substance — the fence matches by suffix and app.slack.com is inside it — and listed separately because the tripwire reads literals, and because the reason this particular subdomain is named in source deserves to be findable. Slack's sign-in ends on an interstitial offering to launch the desktop app; that page holds no token, so the window walks to the web client once, after the required cookies prove the sign-in worked. Not a workaround for a failed login: it is the last step the owner would otherwise have to know to take, and one of them sat on that page with nothing to press (2026-08-26).",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.slack.webLogin.storageUrl"
    },
    {
      "host": "accounts.google.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift + connect/lib/bridge.mjs PLATFORMS.slack",
      "direction": "bidirectional",
      "purpose": "Google's OAuth sign-in, when the owner chooses \"Sign In With Google\" on Slack's own login page inside the bridge login window.",
      "credential": "the owner's Google password and second factor, typed on Google's own page in that window; this app never reads them",
      "decision": "Slack's login page offers email, Google and Apple, and the window's fence allowed only slack.com -- so the Google button reached slack.com/signin/oauth/google/start and the next hop was cancelled with no error, which reads as a dead button (owner, 2026-08-26). Allowed because it IS Slack's sign-in, not a detour from it. Measured with a fenceless probe that clicked the button and logged every main-frame host; the enumeration stops at Google's \"Email or phone\" screen, because going further means typing an address and a password, which is the owner's to do. A later 2FA hop through a host not listed here will die the same silent way, and the fix is to measure that hop and name it, never to widen this to google.com.",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.slack.webLogin.allowedHosts; widget/src/BridgeLogin.swift navigation fence"
    },
    {
      "host": "accounts.youtube.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift + connect/lib/bridge.mjs PLATFORMS.slack",
      "direction": "bidirectional",
      "purpose": "Google's cross-domain session sync, a hop inside the same Google sign-in.",
      "credential": "none of its own -- it carries Google's session cookies, set by the step before it",
      "decision": "Part of Google's sign-in rather than a separate destination: the probe above logged it between accounts.google.com and the return to Slack. Listed separately because the fence matches hosts, and a reader who finds youtube.com in a login window's allowlist deserves to find the reason next to it.",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.slack.webLogin.allowedHosts"
    },
    {
      "host": "appleid.apple.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift + connect/lib/bridge.mjs PLATFORMS.slack",
      "direction": "bidirectional",
      "purpose": "Apple's sign-in, when the owner chooses \"Apple\" on Slack's login page inside the bridge login window.",
      "credential": "the owner's Apple ID credentials, typed on Apple's own page in that window; this app never reads them",
      "decision": "Same cancelled-navigation bug as Google and fixed in the same commit. One hop, measured the same way: slack.com -> appleid.apple.com. Included because leaving one of Slack's three sign-in buttons dead while fixing the other two is the kind of half-fix that reads as a different bug later.",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.slack.webLogin.allowedHosts"
    },
    {
      "host": "www.google.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift + connect/lib/bridge.mjs PLATFORMS.slack",
      "direction": "bidirectional",
      "purpose": "reCAPTCHA's two iframes — the checkbox and the image challenge — inside Slack's own login page in the bridge login window.",
      "credential": "none. The frames carry the challenge and its answer; the app reads only the response token reCAPTCHA writes into the page after a human passes, which is the receipt the Slack bridge asks for.",
      "decision": "SUBFRAMES ONLY, never the top-level window: BridgeLogin's fence has two lists and this host is on the frame one. That distinction is the decision. Allowing it in the main list would let the window navigate itself to Google in the one webview where the owner types a password. Measured 2026-08-26 on Google's own reCAPTCHA demo page inside a fenced WKWebView: /recaptcha/api2/anchor and /recaptcha/api2/bframe, both subframes; www.gstatic.com never appears because the script is a subresource and the fence does not gate those.",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.slack.webLogin.allowedFrameHosts; widget/src/BridgeLogin.swift fence"
    },
    {
      "host": "www.recaptcha.net",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift + connect/lib/bridge.mjs PLATFORMS.slack",
      "direction": "bidirectional",
      "purpose": "The same reCAPTCHA widget, from Google's alternate domain.",
      "credential": "none, as above.",
      "decision": "NOT OBSERVED, and listed anyway — the one row in this file that is precaution rather than measurement, said plainly so nobody mistakes it for evidence. Google serves the identical widget from this domain for networks where google.com is unreachable, and which one a page gets is decided by the script URL the site loads. Slack loads that script lazily from a bundle, so it was not cheap to determine. If it is never used this row is unused; if it is, its absence would have been an empty challenge box that reproduces on nobody's machine but the owner's.",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.slack.webLogin.allowedFrameHosts"
    },
    {
      "host": "web.telegram.org",
      "kind": "bridge",
      "component": "bridges/ mautrix-telegram + connect/lib/bridge.mjs",
      "direction": "bidirectional",
      "purpose": "Telegram DMs through a local Matrix bridge",
      "credential": "harvested session tokens",
      "decision": "User-enabled source. Off until the user connects it on the connect page, and it reaches only that user's own account. Was UNRECORDED while this lived in a private repo, where `decision` meant 'the owner wrote down that they approved this' — a question with no meaning in a public one.",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.telegram; bridges/docker-compose.yml image dock.mau.dev/mautrix/telegram"
    },
    {
      "host": "www.facebook.com",
      "kind": "bridge",
      "component": "bridges/ mautrix-meta + connect/lib/bridge.mjs",
      "direction": "bidirectional",
      "purpose": "Messenger DMs through a local Matrix bridge",
      "credential": "harvested session cookies (full account bearers)",
      "decision": "SOCIAL-BRIDGES-PLAN.md, owner-requested 2026-08-21, 'accept the account risk'",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.messenger; bridges/docker-compose.yml",
      "note": "The bare domain is declared separately under login-webview: the embedded login navigates the apex and its subdomains, not just this www host."
    },
    {
      "host": "www.instagram.com",
      "kind": "bridge",
      "component": "bridges/ mautrix-meta:ig + connect/lib/bridge.mjs",
      "direction": "bidirectional",
      "purpose": "Instagram DMs through a local Matrix bridge",
      "credential": "harvested session cookies (full account bearers)",
      "decision": "SOCIAL-BRIDGES-PLAN.md, owner-requested 2026-08-21",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.instagram; bridges/docker-compose.yml",
      "note": "The bare domain is declared separately under login-webview: the embedded login navigates the apex and its subdomains, not just this www host."
    },
    {
      "host": "linkedin.com",
      "kind": "bridge",
      "component": "bridges/ mautrix-linkedin + connect/lib/bridge.mjs",
      "direction": "inbound-fetch",
      "purpose": "LinkedIn DMs through a local Matrix bridge — replaced the hand-unzipped CSV export (owner, 2026-08-25)",
      "credential": "the owner's own LinkedIn session, captured by the login webview",
      "decision": "Owner asked for LinkedIn to sync like every other social source, 2026-08-25. Same posture as the Meta and X bridges: read-only, no host port, double puppeting off.",
      "evidence": "bridges/docker-compose.yml mautrix-linkedin; connect/lib/bridge.mjs PLATFORMS.linkedin"
    },
    {
      "host": "www.linkedin.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift (fence from connect/lib/bridge.mjs)",
      "direction": "inbound-fetch",
      "purpose": "LinkedIn cookie + request-header login, in the fenced in-app window",
      "credential": "the owner's own LinkedIn session",
      "decision": "Same decision as the Meta and X login webviews: the owner logs in to their own account, in a window fenced to that platform's hosts.",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.linkedin.webLogin.allowedHosts"
    },
    {
      "host": "x.com",
      "kind": "bridge",
      "component": "bridges/ mautrix-twitter + connect/lib/bridge.mjs",
      "direction": "bidirectional",
      "purpose": "X/Twitter DMs through a local Matrix bridge",
      "credential": "harvested auth_token + ct0 cookies (full account bearers)",
      "decision": "SOCIAL-BRIDGES-PLAN.md:112, owner decision 2026-08-21, in scope; shipped 2026-08-22",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.twitter; bridges/docker-compose.yml",
      "note": "ALSO reached as a login-webview: the in-app login loads x.com/login and harvests auth_token + ct0. Same host, two capabilities."
    },
    {
      "host": "facebook.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift (fence from connect/lib/bridge.mjs)",
      "direction": "outbound",
      "purpose": "Messenger cookie login, and the host Instagram's login hands off to",
      "credential": "the owner types their own password into the platform's real page; session cookies are harvested from the isolated non-persistent store",
      "decision": "owner decision 2026-08-21 (SOCIAL-BRIDGES-PLAN.md) covers the cookie login itself; these hosts are the navigation fence that flow runs inside, declared 2026-08-23 when the fence moved out of BridgeLogin.swift into connect/lib/bridge.mjs PLATFORMS.webLogin",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.*.webLogin.allowedHosts; enforced in widget/src/BridgeLogin.swift decidePolicyFor; asserted in connect/test/bridgePolicy.test.mjs"
    },
    {
      "host": "instagram.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift (fence from connect/lib/bridge.mjs)",
      "direction": "outbound",
      "purpose": "Instagram cookie login",
      "credential": "the owner types their own password into the platform's real page; session cookies are harvested from the isolated non-persistent store",
      "decision": "owner decision 2026-08-21 (SOCIAL-BRIDGES-PLAN.md) covers the cookie login itself; these hosts are the navigation fence that flow runs inside, declared 2026-08-23 when the fence moved out of BridgeLogin.swift into connect/lib/bridge.mjs PLATFORMS.webLogin",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.*.webLogin.allowedHosts; enforced in widget/src/BridgeLogin.swift decidePolicyFor; asserted in connect/test/bridgePolicy.test.mjs"
    },
    {
      "host": "messenger.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift (fence from connect/lib/bridge.mjs)",
      "direction": "outbound",
      "purpose": "Messenger cookie login can land here mid-flow",
      "credential": "the owner types their own password into the platform's real page; session cookies are harvested from the isolated non-persistent store",
      "decision": "owner decision 2026-08-21 (SOCIAL-BRIDGES-PLAN.md) covers the cookie login itself; these hosts are the navigation fence that flow runs inside, declared 2026-08-23 when the fence moved out of BridgeLogin.swift into connect/lib/bridge.mjs PLATFORMS.webLogin",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.*.webLogin.allowedHosts; enforced in widget/src/BridgeLogin.swift decidePolicyFor; asserted in connect/test/bridgePolicy.test.mjs"
    },
    {
      "host": "meta.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift (fence from connect/lib/bridge.mjs)",
      "direction": "outbound",
      "purpose": "Meta account center / 2FA step inside the Messenger and Instagram logins",
      "credential": "the owner types their own password into the platform's real page; session cookies are harvested from the isolated non-persistent store",
      "decision": "owner decision 2026-08-21 (SOCIAL-BRIDGES-PLAN.md) covers the cookie login itself; these hosts are the navigation fence that flow runs inside, declared 2026-08-23 when the fence moved out of BridgeLogin.swift into connect/lib/bridge.mjs PLATFORMS.webLogin",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.*.webLogin.allowedHosts; enforced in widget/src/BridgeLogin.swift decidePolicyFor; asserted in connect/test/bridgePolicy.test.mjs"
    },
    {
      "host": "twitter.com",
      "kind": "login-webview",
      "component": "widget/src/BridgeLogin.swift (fence from connect/lib/bridge.mjs)",
      "direction": "outbound",
      "purpose": "X cookie login; twitter.com still redirects to x.com and some flows land there first",
      "credential": "the owner types their own password into the platform's real page; session cookies are harvested from the isolated non-persistent store",
      "decision": "owner decision 2026-08-21 (SOCIAL-BRIDGES-PLAN.md) covers the cookie login itself; these hosts are the navigation fence that flow runs inside, declared 2026-08-23 when the fence moved out of BridgeLogin.swift into connect/lib/bridge.mjs PLATFORMS.webLogin",
      "evidence": "connect/lib/bridge.mjs PLATFORMS.*.webLogin.allowedHosts; enforced in widget/src/BridgeLogin.swift decidePolicyFor; asserted in connect/test/bridgePolicy.test.mjs"
    },
    {
      "host": "cdn.jsdelivr.net",
      "kind": "model-asset",
      "component": "widget/voice/scripts/fetch-models.mjs",
      "direction": "inbound-fetch",
      "purpose": "vendor asset download at build time",
      "credential": "none",
      "decision": "Build/setup-time model download, not reachable from the running app. Model weights are hundreds of megabytes and are fetched rather than committed. Was UNRECORDED for the same reason as the sources above.",
      "evidence": "widget/voice/scripts/fetch-models.mjs",
      "note": "build-time only; not reachable from the shipped app"
    },
    {
      "host": "download.moonshine.ai",
      "kind": "model-asset",
      "component": "widget/voice/scripts/fetch-models.mjs",
      "direction": "inbound-fetch",
      "purpose": "Moonshine speech-to-text model download at build time",
      "credential": "none",
      "decision": "Build/setup-time model download, not reachable from the running app. Model weights are hundreds of megabytes and are fetched rather than committed. Was UNRECORDED for the same reason as the sources above.",
      "evidence": "widget/voice/scripts/fetch-models.mjs",
      "note": "build-time only; not reachable from the shipped app"
    },
    {
      "host": "huggingface.co",
      "kind": "model-asset",
      "component": "widget/voice/scripts/fetch-models.mjs (build), ops/setup-llm.sh (setup)",
      "direction": "inbound-fetch",
      "purpose": "download the Kokoro voice and llama model weights",
      "credential": "HF_TOKEN for gated repos",
      "decision": "Build/setup-time model download, not reachable from the running app. Model weights are hundreds of megabytes and are fetched rather than committed. Was UNRECORDED for the same reason as the sources above.",
      "evidence": "widget/src/ModelSetup.swift:33 -- the APP downloads the answer model itself (URLSessionDownloadTask), which is the largest of these and was the one the privacy policy denied; widget/voice/scripts/fetch-models.mjs and ops/setup-llm.sh (scripts the owner runs); site/notices/index.html:314,318,334 (links the notices page hands the visitor). Three contexts, one host -- filtering by kind alone under-counts both the site and the app.",
      "note": "Until 4128ae2c this was ALSO a RUNTIME path in the shipped app: widget/ui/ear-main.js called createVoice() without localOnly, so a missing vendored voice file fell through to kokoro-js's hardcoded huggingface.co fetch. Fixed in source; the notarized DMG still carries the old behaviour until re-cut."
    },
    {
      "host": "dock.mau.dev",
      "kind": "registry",
      "component": "bridges/docker-compose.yml + ops/setup-bridges.sh + ops/prefetch-bridges.sh",
      "direction": "pull",
      "purpose": "all seven mautrix bridge images",
      "credential": "none",
      "decision": "implied by the bridge decisions above; never enumerated as egress before this file",
      "evidence": "bridges/docker-compose.yml, the seven image: dock.mau.dev/... lines",
      "note": "The runtime Compose file pins every bridge by digest. Telegram setup and prefetch use that same digest; keep the remaining setup/prefetch rows aligned before enabling their flows."
    },
    {
      "host": "ghcr.io",
      "kind": "registry",
      "component": "bridges/docker-compose.yml",
      "direction": "pull",
      "purpose": "Synapse homeserver image",
      "credential": "none",
      "decision": "implied by the bridge decisions above; never enumerated as egress before this file",
      "evidence": "bridges/docker-compose.yml image ghcr.io/element-hq/synapse:v1.140.0"
    },
    {
      "host": "accounts.google.com",
      "kind": "user-browser-link",
      "component": "ops/gcal-auth.mjs",
      "direction": "none",
      "purpose": "the OAuth consent URL printed for the owner to open",
      "credential": "none -- this process opens no socket here",
      "decision": "n/a",
      "evidence": "ops/gcal-auth.mjs"
    },
    {
      "host": "brew.sh",
      "kind": "user-browser-link",
      "component": "ops/setup-llm.sh",
      "direction": "none",
      "purpose": "named inside an error message telling the operator where to get Homebrew",
      "credential": "none -- this is a string printed to stderr, not a call",
      "decision": "n/a",
      "evidence": "ops/setup-llm.sh:117"
    },
    {
      "host": "cloud.ouraring.com",
      "kind": "user-browser-link",
      "component": "ops/oura-auth.mjs; widget/src/Bridge.swift openExternal allowlist",
      "direction": "none",
      "purpose": "the OAuth consent URL ops/oura-auth.mjs opens in the owner's browser, the app-registration page (cloud.ouraring.com/oauth/applications) the same script's instructions tell the owner to fill in by hand, and the Oura help link in the widget's openExternal allowlist",
      "credential": "none -- this process opens no socket here",
      "decision": "n/a",
      "evidence": "ops/oura-auth.mjs AUTHORIZE_URL; widget/src/Bridge.swift allowedExternal",
      "note": "The token exchange and refresh do NOT run here: connectors/lib/ouraClient.mjs posts them to api.ouraring.com (OURA_TOKEN_URL), which is its own entry above. An earlier revision of this file declared this host `api`, component connectors/lib/ouraClient.mjs, carrying the oura refresh token -- no code in this repo has ever sent a credential to it."
    },
    {
      "host": "console.cloud.google.com",
      "kind": "user-browser-link",
      "component": "ops/gcal-auth.mjs",
      "direction": "none",
      "purpose": "setup instructions printed for the owner",
      "credential": "none -- this process opens no socket here",
      "decision": "n/a",
      "evidence": "ops/gcal-auth.mjs"
    },
    {
      "host": "docs.mau.fi",
      "kind": "user-browser-link",
      "component": "widget/ui/connections.js BRIDGE_HELP (discord, slack)",
      "direction": "none",
      "purpose": "mautrix authentication docs, linked from the bridge login panel so the owner can find their Discord token / Slack xoxc+xoxd tokens",
      "credential": "none -- handed to the owner's browser, this process opens no socket",
      "decision": "n/a",
      "evidence": "widget/ui/connections.js BRIDGE_HELP; widget/src/Bridge.swift allowedExternal",
      "note": "Added by the ledger tripwire on the commit that introduced it, which is the point of the tripwire. FOUND AT THE SAME TIME, and since fixed: this host was missing from Bridge.swift's allowedExternal, an exact-match allowlist (Bridge.swift's case \"openExternal\" handler), so both links failed with 'url not in allowlist'. Both URLs are now in allowedExternal."
    },
    {
      "host": "my.telegram.org",
      "kind": "user-browser-link",
      "component": "widget/src/Bridge.swift openExternal allowlist",
      "direction": "none",
      "purpose": "send the owner to Telegram to register their OWN api_id/api_hash -- mautrix-telegram refuses to start on the example pair it ships, and Telegram bans app ids shared across unrelated accounts, so each install registers its own (owner, 2026-08-25)",
      "credential": "none -- this process opens no socket here",
      "decision": "n/a",
      "evidence": "widget/src/Bridge.swift allowedExternal https://my.telegram.org/apps"
    },
    {
      "host": "granola.ai",
      "kind": "user-browser-link",
      "component": "widget/src/Bridge.swift openExternal allowlist",
      "direction": "none",
      "purpose": "send the owner to Granola to fetch an API key",
      "credential": "none -- this process opens no socket here",
      "decision": "n/a",
      "evidence": "widget/src/Bridge.swift allowedExternal https://granola.ai"
    },
    {
      "host": "myaccount.google.com",
      "kind": "user-browser-link",
      "component": "widget/src/Bridge.swift openExternal allowlist, connect/lib/page.mjs",
      "direction": "none",
      "purpose": "send the owner to their own app-passwords page",
      "credential": "none -- this process opens no socket here",
      "decision": "n/a",
      "evidence": "widget/src/Bridge.swift allowedExternal https://myaccount.google.com/apppasswords"
    },
    {
      "host": "www.notion.so",
      "kind": "user-browser-link",
      "component": "widget/src/Bridge.swift openExternal allowlist",
      "direction": "none",
      "purpose": "send the owner to their Notion integrations page",
      "credential": "none -- this process opens no socket here",
      "decision": "n/a",
      "evidence": "widget/src/Bridge.swift allowedExternal https://www.notion.so/my-integrations"
    },
    {
      "host": "fonts.googleapis.com",
      "kind": "website",
      "component": "site/index.html",
      "direction": "inbound-fetch",
      "purpose": "the IBM Plex Mono and Caveat stylesheet the landing page links",
      "credential": "none",
      "decision": "Public marketing site. The visitor's browser loads this, not any Intaglio Labs process; it carries no credential and sees only that someone opened the page.",
      "evidence": "site/index.html:13 <link href=\"https://fonts.googleapis.com/css2?family=IBM+Plex+Mono\u2026&family=Caveat\u2026\" rel=\"stylesheet\">"
    },
    {
      "host": "fonts.gstatic.com",
      "kind": "website",
      "component": "site/index.html",
      "direction": "inbound-fetch",
      "purpose": "the font files that stylesheet then pulls",
      "credential": "none",
      "decision": "Public marketing site. The visitor's browser loads this, not any Intaglio Labs process; it carries no credential and sees only that someone opened the page.",
      "evidence": "site/index.html:10 <link rel=\"preconnect\" href=\"https://fonts.gstatic.com\" crossorigin>"
    },
    {
      "host": "www.character.vc",
      "kind": "website",
      "component": "site/index.html",
      "direction": "outbound-link",
      "purpose": "the backer credit under the download button",
      "credential": "none",
      "decision": "Public marketing site. A link the visitor may click; nothing is fetched unless they do. No credential, no automatic request.",
      "evidence": "site/index.html:406,590 backed by <a href=\"https://www.character.vc/\"> \u2014 once per board"
    },
    {
      "host": "www.linkedin.com",
      "kind": "website",
      "component": "site/index.html",
      "direction": "outbound-link",
      "purpose": "the two builder credits under the download button",
      "credential": "none",
      "decision": "Public marketing site. A link the visitor may click; nothing is fetched unless they do. No credential, no automatic request.",
      "evidence": "site/index.html:406,590 built by <a href=\"https://www.linkedin.com/in/\u2026\"> \u2014 once per board"
    },
    {
      "host": "github.com",
      "kind": "registry",
      "component": "ops/fetch-bridges.mjs + bridges/native.json; also site/index.html",
      "direction": "inbound-fetch",
      "purpose": "download the prebuilt mautrix bridge binaries named in bridges/native.json, replacing the container images pulled from dock.mau.dev. Separately and unchanged, the upstream project links the marketing site hands to a visitor's browser.",
      "credential": "none -- unauthenticated public release assets",
      "decision": "Replaces a `docker pull` with an equivalent fetch of the same upstream software, so the posture is unchanged: upstream artifacts reach the owner's machine from upstream, and this repo conveys none of them. Every asset is pinned by sha256 from the release's own sha256sums.txt, and a mismatch is deleted rather than used. Was kind=website while only the marketing site linked here; the product fetches now, so the stronger kind wins.",
      "evidence": "site/notices/index.html:321,329 (moonshine and silero-vad repository links) and site/index.html:241,405,427,589 (the download buttons, which point at this project's own releases \u2014 header and try-now, once per board) and site/index.html:399,584 (the \"open sourced OS\" link under the prompts headline, once per board)"
    },
    {
      "host": "www.pexels.com",
      "kind": "website",
      "component": "site/notices/index.html",
      "direction": "outbound-link",
      "purpose": "the Pexels licence and homepage, cited where the landing-page photographs are credited",
      "credential": "none",
      "decision": "Public marketing site. A link the visitor may click; nothing is fetched unless they do. The photographs THEMSELVES are not this path -- they are resized copies served from this origin under site/photos/, deliberately not hot-linked, so opening the landing page sends Pexels no request and tells them nothing about who visited.",
      "evidence": "site/notices/index.html \u00a7Landing-page photographs -- https://www.pexels.com/ and https://www.pexels.com/license/"
    },
    {
      "host": "api.github.com",
      "kind": "website",
      "component": "site/index.html",
      "direction": "inbound-fetch",
      "purpose": "the download counter under the try-now button reads the release assets' real download_count",
      "credential": "none",
      "decision": "Public marketing site, and note this is a FETCH, not a link -- the visitor's browser calls it on page load, so GitHub sees their IP and user agent whether or not they ever click download. That is the same exposure the page already has to Google Fonts, and site/privacy/index.html discloses it in the same sentence. It is here rather than a counter of our own on purpose: a click tally would need a server this project does not have, would be a number about visitors, and would be inflatable by anyone with curl. This reads a number GitHub already publishes and records nothing.",
      "evidence": "site/index.html -- COUNTER.endpoint, https://api.github.com/repos/intaglio-labs/intagliolabs/releases"
    },
    {
      "host": "intaglio.io",
      "kind": "website",
      "component": "site/index.html",
      "direction": "self",
      "purpose": "the site's own canonical URL and link-preview image, named absolutely in the og: and twitter: tags",
      "credential": "none",
      "decision": "This is the site talking about itself, not reaching anywhere new -- og:url and og:image have to be absolute because a crawler resolves them without a document base, so the origin appears as a literal here. No visitor request goes anywhere it was not already going. Listed because the tripwire scans for host literals, not for intent, and a reader who finds intaglio.io in the source should be able to see it accounted for rather than wonder.",
      "evidence": "site/index.html -- og:url, og:image, twitter:image (https://intaglio.io/ and https://intaglio.io/og.png)"
    },
    {
      "host": "www.apache.org",
      "kind": "website",
      "component": "site/index.html",
      "direction": "outbound-link",
      "purpose": "the Apache-2.0 licence text the notices page cites",
      "credential": "none",
      "decision": "Public marketing site. A link the visitor may click; nothing is fetched unless they do. No credential, no automatic request.",
      "evidence": "site/notices/index.html:111,303 http://www.apache.org/licenses/LICENSE-2.0"
    },
    {
      "host": "mau.fi",
      "kind": "website",
      "component": "site/privacy/index.html",
      "direction": "outbound",
      "purpose": "mautrix project homepage",
      "credential": null,
      "decision": "allowed",
      "evidence": "site/privacy/index.html -- §6 links the mautrix project when naming the bridges that reach the chat platforms, so the reader can see whose software runs on their Mac"
    },
    {
      "host": "www.docker.com",
      "kind": "user-browser-link",
      "component": "widget/src/Bridge.swift openExternal allowlist, widget/ui/connections.js, widget/ui/connector-tile.js",
      "direction": "none",
      "purpose": "send the owner to download Docker Desktop when the social bridges cannot start because it is not installed -- reached only from the nobridge notice's button, and only after openApp reports the bundle absent",
      "credential": "none -- this process opens no socket here",
      "decision": "n/a",
      "evidence": "widget/src/Bridge.swift allowedExternal https://www.docker.com/products/docker-desktop/"
    },
    {
      "host": "release-assets.githubusercontent.com",
      "kind": "registry",
      "component": "ops/fetch-bridges.mjs (redirect target, not a source literal)",
      "direction": "inbound-fetch",
      "purpose": "where a github.com release-asset URL actually serves the bytes -- GET /releases/download/... answers 302 to this host, so a fetch of a bridge binary reaches it whether or not the name appears in any source file",
      "credential": "none -- the redirect carries a short-lived signed query string",
      "decision": "Declared by hand because connectors/test/egress.test.mjs matches host LITERALS and a redirect target is never one. Measured 2026-08-29: curl -IL on a mautrix release asset returns 302 to this host. Same decision as the github.com entry above -- it is one fetch, and listing only the host that appears in source would leave the ledger true by wording and false in substance, which its own _readme calls the failure this file exists to prevent.",
      "evidence": "ops/fetch-bridges.mjs download() follows redirects; bridges/native.json base https://github.com/mautrix"
    }
  ],
  "_loopback": [
    "Not egress, listed so the test's allowlist is complete and so nobody",
    "mistakes them for hosts: 127.0.0.1 / localhost / [::1], hermes on :8789,",
    "llama-server on :8080, connect on :8788, and `synapse`, the",
    "compose-internal DNS name the bridge connector uses inside the docker",
    "network. connectors/ opens NO listener of any kind; its sockets are",
    "outbound only."
  ]
}

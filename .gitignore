# Dependencies
node_modules/

# Build output
widget/build/
widget/voice/public/
widget/voice/node_modules/
dist/

# Runtime state and secrets never belong in the repo. Everything this system
# stores lives under ~/.hazlie (0700), outside any checkout, by design.
.env
.env*.local
*.log

# macOS
.DS_Store

# Firebase Hosting deploy cache. The site moved to GitHub Pages on 2026-08-24
# and firebase.json/.firebaserc are gone; this line stays only so a stale local
# cache from before the move cannot be committed by someone who still has one.
.firebase/
